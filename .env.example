# The CUDA build of GROMACS, if you installed it with scripts/setup-gromacs-cuda.sh.
# Leave unset to fall back to whatever `gmx` is on PATH.
# VERVAIN_GMX=$HOME/opt/gromacs/bin/gmx

# Override where structures and trajectories are written. Defaults to ./data.
# VERVAIN_DATA=/mnt/d/vervain-data
