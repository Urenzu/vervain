# Coarse-grained MD pipeline. Runs under WSL (Ubuntu), not native Windows —
# GROMACS and martinize2 both assume a POSIX toolchain.
#
# GROMACS itself is not a pip package. Install it with:
#     bash scripts/setup-gromacs-cuda.sh
# The Ubuntu package works for system building but is compiled without GPU
# support and with SSE4.1 SIMD, so it is not usable for production runs.

# Atomistic -> coarse-grained conversion. This is martinize2.
vermouth>=0.9.7

# Trajectory and structure handling.
MDAnalysis>=2.7
numpy>=2.0

# Structure manipulation: grafting complete domains onto incomplete ones,
# superposition, chain surgery.
gemmi>=0.6

pyyaml>=6.0
pytest>=8.0
