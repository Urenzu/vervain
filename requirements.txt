# Verified on Linux (Ubuntu 26.04 / CPython 3.14, manylinux_2_28 wheels) and
# Windows (CPython 3.13). Both binary deps — libroadrunner and python-libsbml —
# publish cp311..cp314 wheels for manylinux and win_amd64, so no compiler is
# needed on either platform.
#
# bionetgen ships the BioNetGen binaries (bng-linux / bng-mac / bng-win) that
# PySB shells out to for reaction-network generation. It is a hard runtime
# dependency, not a convenience: PySB cannot produce ODEs without it.
pysb==1.17.0
bionetgen==0.8.7
python-libsbml==5.21.1
libroadrunner==2.10.0
numpy>=2.0
scipy>=1.14
pyyaml>=6.0
websockets>=13.0
pytest>=8.0
